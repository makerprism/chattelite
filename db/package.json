{
  "name": "rust-simple-chat-schema",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx tsc && node dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^7.0.5",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "dotenv": "^10.0.0",
    "postgres-migrations": "^5.3.0"
  }
}
