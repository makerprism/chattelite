import { T } from 'gen-types';

export const t = {
    ConversationId: T("ConversationId"),

    Username: T("Username"),
    DateTime: T("DateTime"),
};


export const it = {
   
};

export const ot = {
    
    Message: T("Message"),
};
