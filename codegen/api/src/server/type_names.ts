import { T } from 'gen-types';

export const t = {
    UserId: T("UserId"),
    ConversationId: T("ConversationId"),

    //DateTime: T("DateTime"),

};


export const it = {
   
};

export const ot = {
    
};
